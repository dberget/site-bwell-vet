// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://dberget.github.io',
  base: '/site-engine',
  output: 'static',
});
